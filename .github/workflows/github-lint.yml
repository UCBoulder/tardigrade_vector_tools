name: Lints on pull request
run-name: ${{ github.actor }} is linting a pull request
on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
    branches:
      - dev
      - main
jobs:
  lint:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        build_type: [Release]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
            fetch-depth: 0
      - name: MiniConda setup
        uses: conda-incubator/setup-miniconda@v2
        with:
            miniforge-variant: Miniforge3
            channels: conda-forge,defaults
            channel-priority: true
            auto-activate-base: false
            activate-environment: "lint-environment"
      - name: Conda environment
        shell: bash -el {0}
        run: conda install --file environment.txt --yes --channel conda-forge
      - name: CMake linter
        shell: bash -el {0}
        run: gersemi CMakeLists.txt src/ docs/ --check --diff
